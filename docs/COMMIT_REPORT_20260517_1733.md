# Commit Report

## Summary

- Project: /home/beou/IdeaProjects/expat8
- Branch: x51-commit/20260517-1733-article-worker-fix
- Mode: single project
- Timestamp: 2026-05-17T17:33 (local)
- Branch decision: new branch (large, multi-area changes)
- Commit title: fix(backend/worker): add expat8-worker to compose; fix persistArticleVocabulary source_article_id bug
- Push target: origin/x51-commit/20260517-1733-article-worker-fix

## Uncommitted Audit

- Backend: `persistArticleVocabulary` used non-existent `source_article_id` column on `workplace_sentences`; fixed to use `article_workplace_sentences` junction table
- Backend: log archive store (new file), config, runtime, app.js updates for log archive API
- Dashboard: new ops workspace (`/ops`, `/ops/logs`, `/ops/logs/[id]`); nav, globals.css, layout changes
- Mobile: log upload (`backend_api_client.dart`, `word_repository.dart`, `logs_screen.dart`); workplace sentence session; learning screen updates; objectbox model changes
- Deployment: `expat8-worker` service added to `deployment/worker-z440/docker-compose.yml`; default backend image updated to `20260517.1733`

## Files Planned For Commit

- `backend/src/postgres_word_store.js`: fix `persistArticleVocabulary` — replace invalid `source_article_id` column reference with `article_workplace_sentences` junction table cleanup
- `backend/src/app.js`: log archive API routes and admin access
- `backend/src/config.js`: log archive env settings, ADMIN_API_TOKENS
- `backend/src/runtime.js`: log archive store wiring
- `backend/src/log_archive_store.js`: new — log archive storage
- `backend/test/log_archive_store.test.js`: new tests
- `backend/test/log_archives_api.test.js`: new tests
- `backend/test/config.test.js`: log archive config tests
- `expat8-dashboard/src/app/ops/`: new ops workspace pages
- `expat8-dashboard/src/lib/ops-data.ts`, `ops.js`, `ops.d.ts`, `nav.js`, `nav.d.ts`: ops data access
- `expat8-dashboard/src/app/globals.css`, `layout.tsx`, `components/SidebarNav.tsx`: nav and style
- `expat8-dashboard/package.json`: dependency updates
- `mobile/lib/src/api/backend_api_client.dart`: log upload
- `mobile/lib/src/data/word_repository.dart`, `local_database.dart`, `local_database_entities.dart`: word and sentence data
- `mobile/lib/src/session/learning_session_controller.dart`, `workplace_sentence_session_controller.dart`: session logic
- `mobile/lib/src/ui/logs_screen.dart`, `learning_screen.dart`, `workplace_sentence_screen.dart`: UI updates
- `mobile/lib/src/models/learning_progress.dart`, `learning_gesture_surface.dart`, `learning_history_screen.dart`, `learning_progress_stats_screen.dart`: new model and screens
- `mobile/lib/objectbox-model.json`, `objectbox.g.dart`: ObjectBox schema update
- `mobile/test/*`: updated and new tests
- `deployment/worker-z440/docker-compose.yml`: expat8-worker service added; backend image → 20260517.1733
- `openspec/changes/expat8-dashboard-admin-ops/`: OpenSpec change artifacts
- `openspec/changes/mobile-learning-swipe-workflows-and-stats/`: OpenSpec change artifacts
- `openspec/changes/archive/2026-05-17-fix-swipe-rtl-card-repeat/`: archived change
- Various docs and openspec archive updates

## Detailed Changes

- `backend/src/postgres_word_store.js` (line 1522-1527): replaced `DELETE FROM workplace_sentences WHERE source_article_id = $1` with correct pattern using `article_workplace_sentences` junction table, matching the pattern at lines 1657-1661
- `deployment/worker-z440/docker-compose.yml`: added `expat8-worker` service (same image as backend, `npm run start:worker` command, same env minus PORT); updated default backend image to `20260517.1733`

## Affected Flows

- **Article processing pipeline**: `persistArticleVocabulary` now correctly cleans up existing workplace sentences before reprocessing — fixes dead-lettering of all new articles
- **Deployment**: `expat8-worker` is now a first-class service in the Z440 deployment compose, no longer an orphan container
- **Log archive**: mobile can upload logs to backend; dashboard ops workspace displays them

## Impact And Risks

- Impact: article processing was completely broken (every new article became dead_lettered after 3 attempts); now fixed and verified end-to-end
- Risk: low — the fix uses the same pattern already used in the codebase (lines 1657-1661)
- Mitigation: 185/185 backend tests pass; article processed successfully in production (extracted 11 vocab items, 0 rejections)

## Untracked Files

- `backend/src/log_archive_store.js`: staged
- `backend/test/log_archive_store.test.js`: staged
- `backend/test/log_archives_api.test.js`: staged
- `expat8-dashboard/src/app/ops/`: staged
- `expat8-dashboard/src/lib/nav.d.ts`, `nav.js`, `ops-data.ts`, `ops.d.ts`, `ops.js`: staged
- `expat8-dashboard/test/`: staged
- `mobile/lib/src/models/learning_progress.dart`: staged
- `mobile/lib/src/ui/learning_gesture_surface.dart`, `learning_history_screen.dart`, `learning_progress_stats_screen.dart`: staged
- `openspec/changes/archive/2026-05-17-fix-swipe-rtl-card-repeat/`: staged
- `openspec/changes/expat8-dashboard-admin-ops/`: staged
- `openspec/changes/mobile-learning-swipe-workflows-and-stats/`: staged
- `openspec/specs/mobile-local-card-dedup/`: staged

## Sensitive/Risk Review

- No secrets or tokens in changed files
- Deployment compose updated with new image tag (non-destructive)
- ObjectBox generated files (binary-safe, checked)

## Verification

```bash
cd backend && npm test
```

Result: 185/185 pass, 0 fail

```bash
# Production smoke — article processing
# Reset dead_lettered job → pending_processing → worker picked up in <1s → status: processed
# extracted_count: 11, accepted_count: 11, rejected_count: 0
```

## Commit Comments

Fixes article processing pipeline broken by `source_article_id` column reference on
`workplace_sentences` (column does not exist in schema). The fix uses the existing
`article_workplace_sentences` junction table — same pattern already used at line 1657.

Also adds `expat8-worker` as a first-class service in the Z440 deployment compose
(previously ran as an orphan from an old compose version with a stale image).

Backend image tag: 20260517.1733 (built and pushed to docker.x51.vn).
