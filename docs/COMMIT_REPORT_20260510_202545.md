# Commit Report

## Summary

- Project: `/home/beou/IdeaProjects/expat8`
- Branch: `x51-commit/20260510-expat8-snapshot`
- Mode: single project snapshot
- Timestamp: `20260510_202545`
- Branch decision: new branch, because the worktree spans backend, mobile, deployment, OpenSpec, docs, and generated artifacts
- Commit title: `chore(repo): snapshot expat8 deployment and feature work`
- Push target: `origin/x51-commit/20260510-expat8-snapshot`

## Uncommitted Audit

- Backend runtime and persistence changes for speaking events, article processing, and SRS selection
- Mobile speaking sync/drill lifecycle updates
- Local Compose deployment update for the article worker
- OpenSpec spec sync and archive state changes
- Deployment notes and migration artifact additions
- `.wolf` session state updates from active work

## Files Planned For Commit

- `backend/src/*.js`, `backend/test/*.js`: backend behavior and tests
- `backend/db/schema.sql`, `backend/db/migrations/20260510_srs_review_index.sql`: schema and index support
- `mobile/lib/**/*.dart`: speaking prompt sync and drill fixes
- `docker-compose.yml`: local worker deployment wiring
- `contracts/api.md`: API contract alignment
- `openspec/specs/**/*.md`: synced main specs
- `openspec/changes/archive/**`: archived completed changes
- `docs/20260510-article-processing-worker-deployment.md`: worker deployment notes
- `.wolf/*`: workspace/session metadata changes already present in worktree

## Detailed Changes

- `backend/src/word_store.js`, `backend/src/postgres_word_store.js`: speaking prompt serialization, article vocabulary persistence, SRS card selection, and article processing behaviors
- `backend/src/app.js`, `backend/src/config.js`, `backend/src/runtime.js`: request/config/runtime wiring for speaking events and worker options
- `backend/src/vocabulary_enrichment_adapter.js`: fallback stub marking
- `backend/test/api.test.js`, `backend/test/word_store.test.js`: coverage for new backend behaviors
- `mobile/lib/main.dart`, `mobile/lib/src/data/local_database.dart`, `mobile/lib/src/speaking/*`: speaking prompt lifecycle, stale sync pruning, drill counting fixes
- `docker-compose.yml`: adds local `article-worker` service
- `docs/20260510-article-processing-worker-deployment.md`: rollout, rollback, and smoke-test notes
- `openspec/specs/*`: syncs architecture findings into main specs
- `openspec/changes/archive/*`: archives completed OpenSpec changes

## Change Content Summary

- Backend now handles speaking prompt field mapping, article vocabulary idempotency, stub fallback marking, and SRS review indexing
- Mobile now keeps speaking prompts synchronized across app lifecycle and fixes drill counting/state issues
- Local Docker compose now includes an article processing worker
- OpenSpec main specs are synchronized with completed change work
- Deployment notes document live worker behavior and rollback guidance

## Affected Flows

- Runtime flow: backend request handling, article processing worker loop, speaking prompt sync
- User/API flow: speaking prompts, learning cards, article vocabulary, study events
- Background/deployment/dev workflow: Docker Compose local stack, Z440 deployment notes, OpenSpec archive state

## Impact And Risks

- Impact: broad, but coherent snapshot across backend, mobile, deployment, and docs
- Risk: `.wolf/*` metadata is included because it was already modified in the worktree and may be environment-specific
- Risk: this commit includes generated/archive/spec files as part of a full snapshot
- Mitigation: branch isolates the snapshot from mainline work

## Untracked Files

- `backend/db/migrations/20260510_srs_review_index.sql`: staged
- `docs/20260510-article-processing-worker-deployment.md`: staged
- `openspec/changes/archive/20260510-deploy-article-processing-worker/`: staged
- `openspec/changes/archive/20260510-fix-architecture-findings/`: staged
- `openspec/specs/speaking-events/`: staged
- `openspec/specs/speaking-prompt-lifecycle/`: staged
- `openspec/specs/srs-card-selection/`: staged

## Sensitive/Risk Review

- Secrets checked: no secret file contents were printed
- Large/generated files: `.wolf/daemon.log`, `.wolf/token-ledger.json`, `.wolf/hooks/_session.json` included as existing workspace state
- Deleted files: archived OpenSpec change files are intentional deletions from active change location

## Verification

```bash
git status --short
git diff --stat .wolf backend contracts docker-compose.yml mobile openspec docs
openspec status --change "deploy-article-processing-worker" --json
ssh -i /home/beou/deployment/worker-z440/ssh/id_ed25519 -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=no beou@10.113.213.9 "docker ps --filter 'label=app=expat8' --format 'table {{.Names}}\t{{.Status}}\t{{.Image}}'"
ssh -i /home/beou/deployment/worker-z440/ssh/id_ed25519 -o IdentitiesOnly=yes -o BatchMode=yes -o StrictHostKeyChecking=no beou@10.113.213.9 "docker logs expat8-worker --tail 30 2>&1"
```

Result:

```text
Working tree has broad intentional modifications.
Z440 worker is running and processing article jobs successfully.
```

## Commit Comments

Snapshot the current expat8 backend/mobile/deployment worktree, including the worker rollout notes, OpenSpec archive state, and supporting schema/spec updates. This keeps the current implementation state together on an isolated branch.

## Notes

- Remaining risk: `.wolf/*` files are included because they are already modified in the workspace snapshot
