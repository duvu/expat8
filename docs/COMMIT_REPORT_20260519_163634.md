# Commit Report

## Summary

- Project: `/home/beou/IdeaProjects/expat8`
- Branch: `x51-commit/20260519-163634-baseline-snapshot`
- Mode: single project
- Timestamp: `2026-05-19T16:36:34` local
- Branch decision: new branch; the worktree is large, mixed, and includes many untracked/generated files, so a baseline snapshot is safer than committing on the active branch
- Commit title: `chore(repo): snapshot current expat8 baseline`
- Push target: `origin/x51-commit/20260519-163634-baseline-snapshot`

## Uncommitted Audit

- Backend: route-module split under `backend/src/routes/*.js`, shared helpers, runtime wiring, worker updates, schema/migration additions, tests, and removal of the old `backend/src/http_utils.js`
- Dashboard: analytics pages under `expat8-dashboard/src/app/analytics/*`, shared time-range helper under `src/lib/time-range.ts`, chart components, ops/system pages, sidebar/navigation, styles, and lockfile/package updates
- Mobile: new submitted-words API models, local database/entity/repository updates, submitted-words UI, ObjectBox regeneration, telemetry tweaks, and tests
- Docs/OpenSpec: `README.md`, `docs/mvp-setup.md`, `.serena` memory note, archived change folders, and multiple OpenSpec spec updates/additions
- Generated/build metadata: `backend/package-lock.json`, `expat8-dashboard/package-lock.json`, `expat8-dashboard/tsconfig.tsbuildinfo`, and ObjectBox generated files

## Files Planned For Commit

- `backend/src/*` and `backend/test/*`: backend refactor, route modules, new submitted-words/study-events work, and test updates
- `backend/db/migrations/*.sql` and `backend/db/schema.sql`: database schema/migration state captured in the baseline
- `expat8-dashboard/src/app/analytics/*`, `src/components/TimeRangeSelector*.tsx`, `src/lib/time-range.ts`, `src/lib/analytics.ts`, `src/components/charts/*`: dashboard analytics and shared time-range split
- `expat8-dashboard/src/app/*`, `src/components/SidebarNav.tsx`, `src/app/globals.css`: dashboard UI and navigation updates
- `mobile/lib/src/*`, `mobile/lib/objectbox-*`, `mobile/test/*`: submitted-words feature, data model regeneration, and tests
- `openspec/*`, `README.md`, `docs/mvp-setup.md`, `.serena/memories/expat8/deploy/2026-05-09-expat8-backend-fresh-deploy.md`: docs, specs, and workspace memory note
- `package-lock.json` files and build metadata: dependency and generated-state snapshot

## Detailed Changes

- Backend work is centered on modularizing routes, adding analytics/study-event-related APIs, and capturing the associated tests and schema/migration state
- Dashboard work splits `parseTimeRange`/`TimeRange` out of the client component into a shared non-client module, then updates the analytics server pages to import from that shared module
- Mobile work adds submitted-word capture and corresponding local data/model/UI updates, along with regenerated ObjectBox code
- OpenSpec and docs changes record the in-progress architecture/spec evolution across backend, dashboard, and mobile areas

## Change Content Summary

- Behavior/config/docs/tests: this is a snapshot of active feature and refactor work across multiple subsystems, plus generated artifacts and documentation/spec updates

## Affected Flows

- Runtime flow: backend request routing, analytics data paths, worker/runtime wiring, and mobile local persistence
- User/API flow: dashboard analytics pages, submitted-words flows, ops/system pages, and backend API surfaces used by the app and dashboard
- Background/deployment/dev workflow: migrations, generated lockfiles/ObjectBox outputs, OpenSpec change tracking, and repo documentation/memory notes

## Impact And Risks

- Impact: captures the current in-progress codebase state as a baseline snapshot so later commits can build from a stable reference point
- Risk: high churn and mixed scope; this commit is not a release candidate and should not be treated as fully validated production state
- Mitigation/notes: branch isolation keeps this snapshot separate from the active branch; no secrets were detected in the audited paths

## Untracked Files

- Backend untracked files under `backend/src/routes/*`, `backend/src/seed_data.js`, `backend/src/store_utils.js`, `backend/src/submitted_word_worker.js`, `backend/db/migrations/*`, and `backend/test/user_submitted_words.test.js`: staged as part of the baseline snapshot
- Dashboard untracked files under `expat8-dashboard/src/app/analytics/*`, `src/app/content/*`, `src/app/system/*`, `src/components/TimeRangeSelector*.tsx`, `src/components/charts/*`, and `src/lib/time-range.ts`: staged as part of the baseline snapshot
- Mobile untracked files under `mobile/lib/src/api/models/*`, `mobile/lib/src/models/submitted_word.dart`, `mobile/lib/src/ui/submitted_words_screen.dart`, and `mobile/test/submitted_words_screen_test.dart`: staged as part of the baseline snapshot
- OpenSpec untracked folders under `openspec/changes/*` and `openspec/specs/*`: staged as part of the baseline snapshot

## Sensitive/Risk Review

- Secrets checked: no `.env`, token, password, or private-key files were included in the audited diff
- Large/generated files: lockfiles, `tsconfig.tsbuildinfo`, and ObjectBox generated files are included intentionally
- Deleted files: `backend/src/http_utils.js` and the archived OpenSpec change folder deletions are intentional and part of the snapshot

## Verification

```bash
git status --short
git diff --stat
git diff --cached --stat
git ls-files --others --exclude-standard
git log --oneline -10
```

Result:

```text
Audit completed successfully; worktree is large and mixed, so additional subsystem verification was skipped for this baseline snapshot.
```

## Commit Comments

Captured the current expat8 worktree as a baseline snapshot on a new branch.

Included backend route modularization and analytics-related API work, dashboard analytics/time-range sharing, mobile submitted-words and ObjectBox updates, plus OpenSpec/docs/generated artifacts.

No additional runtime verification was rerun during this baseline commit; earlier targeted dashboard verification had already passed.

## Notes

- Remaining risk: the snapshot contains active work across multiple subsystems and should be validated per area before release
