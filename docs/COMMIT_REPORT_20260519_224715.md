# Commit Report

## Summary

- Project: `/home/beou/IdeaProjects/expat8`
- Branch: `x51-commit/20260519-224715-baseline-snapshot`
- Mode: single project
- Timestamp: `2026-05-19T22:47:15` local
- Branch decision: new branch; the worktree is large, mixed, and includes many untracked/generated OpenSpec folders plus broad code/docs changes, so a baseline snapshot is safer than committing on the existing branch
- Commit title: `chore(repo): snapshot current expat8 baseline`
- Push target: `origin/x51-commit/20260519-224715-baseline-snapshot`

## Uncommitted Audit

- Backend: `backend/src/app.js`, `backend/src/generation_service.js`, `backend/src/postgres_word_store.js`, `backend/src/routes/user.js`, `backend/src/word_store.js`, and `backend/test/user_submitted_words.test.js` reflect submitted-word flow updates, worker/store plumbing, and API wiring
- Contracts/docs: `contracts/api.md`, `README.md`, `.github/copilot-instructions.md`, and `mobile/README.md` capture updated API/build guidance, including the production mobile backend URL
- Mobile app: `mobile/lib/src/data/local_database.dart`, `mobile/lib/src/data/word_repository.dart`, `mobile/lib/src/models/submitted_word.dart`, `mobile/lib/src/ui/submitted_words_screen.dart`, and related tests reflect the submitted-word/local persistence flow and UI changes
- OpenSpec: many archived change folders were moved/removed and multiple active spec files were updated or added under `openspec/changes/*` and `openspec/specs/*`
- Generated/build metadata: the tree contains large spec/archive churn plus the earlier baseline snapshot already captured generated state; current worktree remains broad and mixed

## Files Planned For Commit

- `backend/src/*` and `backend/test/*`: backend submitted-word / generation flow, store updates, and tests
- `mobile/lib/src/*` and `mobile/test/*`: mobile submitted-word persistence, repository logic, UI, and tests
- `contracts/api.md`, `README.md`, `.github/copilot-instructions.md`, `mobile/README.md`: API/build documentation updates, including production mobile backend URL guidance
- `openspec/changes/*` and `openspec/specs/*`: OpenSpec archival and spec-state snapshot
- `docs/COMMIT_REPORT_20260519_224715.md`: this audit/report file

## Detailed Changes

- `backend/src/word_store.js` and `backend/src/postgres_word_store.js`: submitted-word creation/listing/job handling and persistence paths were extended to match the current in-progress feature work
- `backend/src/routes/user.js`: user routes now expose the submitted-word endpoints and related API shaping used by the mobile flow
- `backend/src/generation_service.js` and `backend/src/app.js`: generation flow and router wiring were updated to support the current backend behavior
- `backend/test/user_submitted_words.test.js`: coverage was added/updated for submitted-word resolution, existing-word reuse, validation, and failure cases
- `mobile/lib/src/data/local_database.dart`, `mobile/lib/src/data/word_repository.dart`, `mobile/lib/src/models/submitted_word.dart`, `mobile/lib/src/ui/submitted_words_screen.dart`: mobile-side local persistence, submission flow, and UI were updated for the new submitted-word behavior
- `mobile/test/backend_api_client_test.dart`, `mobile/test/submitted_words_screen_test.dart`, `mobile/test/word_repository_test.dart`: mobile test coverage was updated alongside the feature work
- `README.md`, `mobile/README.md`, `.github/copilot-instructions.md`: release/build docs now reference `BACKEND_BASE_URL=https://expat8.x51.vn` for the production mobile build instructions
- `contracts/api.md` and OpenSpec files: API and spec documentation were brought in line with the current baseline snapshot

## Change Content Summary

- Behavior/config/docs/tests: this commit snapshots the current in-progress backend, mobile, OpenSpec, and documentation state, including the mobile production backend URL update

## Affected Flows

- Runtime flow: backend request handling, submitted-word generation/storage, worker/store integration, and mobile local persistence
- User/API flow: submitted-word creation/listing, canonical word reuse, and mobile add-word UI
- Background/deployment/dev workflow: OpenSpec archive/spec churn, developer docs, and mobile release build configuration

## Impact And Risks

- Impact: captures the current expat8 codebase state as a baseline for later work
- Risk: high churn and mixed scope; this snapshot is not a release candidate and should not be treated as fully validated production state
- Mitigation/notes: branch isolation keeps the snapshot separate from the prior baseline branch; no secrets were detected in the audited paths

## Untracked Files

- OpenSpec archive/spec folders under `openspec/changes/archive/2026-05-19-*`: staged as part of the baseline snapshot
- OpenSpec active folders under `openspec/changes/instant-add-word-learning/` and `openspec/changes/mobile-self-upgrade/`: staged as part of the baseline snapshot
- OpenSpec spec folders under `openspec/specs/*`: staged as part of the baseline snapshot

## Sensitive/Risk Review

- Secrets checked: no `.env`, token, password, or private-key files were included in the audited diff
- Large/generated files: the snapshot intentionally includes broad OpenSpec archive/spec state and documentation churn; no binary build artifacts were staged
- Deleted files: OpenSpec change-folder removals are intentional and part of the snapshot

## Verification

```bash
git status --short
git diff --stat
git diff --name-only
git ls-files --others --exclude-standard
git log --oneline -10
```

Result:

```text
Audit completed successfully; worktree is large and mixed, so additional subsystem verification was not rerun for this baseline snapshot.
```

## Commit Comments

Captured the current expat8 worktree as a baseline snapshot on a new branch.

Included backend submitted-word and generation flow updates, mobile submitted-word persistence/UI/test updates, API and build-doc updates, and broad OpenSpec archive/spec churn.

No extra runtime verification was rerun during this baseline commit; earlier targeted build/runtime checks had already been completed in prior work.

## Notes

- Remaining risk: the snapshot contains active work across multiple subsystems and should be validated per area before release
