# Commit Report

## Summary

- **Project:** /home/beou/IdeaProjects/expat8
- **Branch:** x51-commit/20260530-phase-2-content-pipeline
- **Mode:** Single project
- **Timestamp:** 2026-05-30 17:26:04
- **Branch decision:** New branch — large cross-surface change (backend + dashboard + mobile + contracts + docs)
- **Commit title:** feat(phase-2): content pipeline health endpoint, ops card, contracts, and Phase 3 roadmap proposal
- **Push target:** origin/x51-commit/20260530-phase-2-content-pipeline

## Uncommitted Audit

- **Backend (src):** `getContentPipelineHealth()` in `word_store.js` + `postgres_word_store.js`; `GET /v1/admin/content-pipeline/health` route in `admin.js`
- **Backend (tests):** 3 new tests in `api.test.js` (admin pipeline health, memorization contract shape, shadowing catalog); 1 new test in `word_store.test.js` (pipeline health unit test)
- **Contracts:** `contracts/api.md` — added `GET /v1/admin/content-pipeline/health` endpoint docs; added `GET /v1/articles`, `POST /v1/articles` (previously undocumented); fixed CORS PATCH; added `POST /v1/admin/speaking-prompts`; added `/v1/content-packs`
- **Dashboard:** `ops-data.ts` — added `ContentPipelineSection`, `ContentPipelineHealth` types and parallel loader; `ops/page.tsx` — added Content Pipeline card with Articles/Memorization/Shadowing stats and failed/stale badges
- **Mobile (test):** `speaking_test.dart` — fixed DB isolation in `postLoopCompletedEvent` test group (unique DB names)
- **Roadmap:** `docs/20260530-project-roadmap-12-month.md` — Phase 2 status updated to `Implementation complete`; Phase 2 implementation status table added (6 Closed gates, 4 Pending manual); active changes table updated; Phase 3 proposal note added
- **Wolf/omo:** `.wolf/anatomy.md`, `.wolf/memory.md` session logs; `.wolf/token-ledger.json`, `.wolf/hooks/_session.json`, `.omo/` runtime state (included as-is)

## Files Planned For Commit

- `backend/src/word_store.js`: `getContentPipelineHealth()` + helper `buildPipelineHealthSection()`
- `backend/src/postgres_word_store.js`: Postgres `getContentPipelineHealth()` + helper `rowToPipelineHealthSection()`; removed 4 pre-existing unused imports
- `backend/src/routes/admin.js`: New `GET /content-pipeline/health` route
- `backend/test/api.test.js`: 3 new integration tests for pipeline health, memorization contract, shadowing catalog
- `backend/test/word_store.test.js`: 1 new unit test for pipeline health
- `contracts/api.md`: New admin endpoint docs + article/CORS/content-packs drift fixes
- `docs/20260530-project-roadmap-12-month.md`: Phase 2 status + Phase 3 proposal entry
- `expat8-dashboard/src/lib/ops-data.ts`: ContentPipelineHealth types + loader
- `expat8-dashboard/src/app/ops/page.tsx`: Content Pipeline ops card
- `mobile/test/speaking_test.dart`: DB isolation fix for speaking test
- `.wolf/anatomy.md`, `.wolf/memory.md`, `.wolf/token-ledger.json`, `.wolf/hooks/_session.json`, `.omo/run-continuation/*.json`: session/memory state

## Detailed Changes

- `backend/src/word_store.js`: In-memory `getContentPipelineHealth()` counts pending/failed across `articleProcessingJobsById`, `memorizationPassagesById`, `shadowingVideosById`. Shadowing pending/failed = 0 (failed imports rejected before persistence).
- `backend/src/postgres_word_store.js`: SQL queries against `article_processing_jobs` (pending_processing/failed), `memorization_passages` (pending_segmentation/segmenting/failed), `shadowing_videos` (0/0 hardcoded). Removed pre-existing stale unused imports to clean LSP.
- `backend/src/routes/admin.js`: `GET /content-pipeline/health` inserted after `GET /speaking/weekly-health`; admin-token protected via existing middleware.
- `backend/test/api.test.js`: Tests cover 403 without admin token, 200 with correct JSON shape, memorization 401/201/200-items, shadowing 400-no-device/401-invalid-bearer/200-anon.
- `contracts/api.md`: Full admin content-pipeline health endpoint documented with warning semantics.
- `expat8-dashboard/src/lib/ops-data.ts`: `loadContentPipelineHealth()` uses `backendFetch`, added to `loadOpsOverview()` Promise.all.
- `expat8-dashboard/src/app/ops/page.tsx`: `pipelineStatus()` / `pipelineLabel()` helpers; Content Pipeline section with Articles/Memorization/Shadowing rows, failed badges.
- `mobile/test/speaking_test.dart`: `databaseCounter` var for unique DB names in `postLoopCompletedEvent` group.
- `docs/20260530-project-roadmap-12-month.md`: Phase 2 marked complete; Phase 3 change entry added.

## Change Content Summary

- **New behavior:** `GET /v1/admin/content-pipeline/health` returns operational backlog counts for articles, memorization passages, and shadowing videos.
- **Dashboard:** `/ops` page now shows Content Pipeline card with per-surface pending/failed counts and staleness warnings.
- **Tests:** Backend 295 pass / 0 fail / 2 skipped; dashboard lint clean + build passes; mobile speaking tests 19/19 pass.
- **Contracts:** API drift from backend investigation corrected (articles, CORS PATCH, content-packs, speaking-prompts).
- **Roadmap:** Phase 2 implementation status fully documented; Phase 3 OpenSpec proposal referenced.

## Affected Flows

- **Runtime flow:** Worker tick unchanged; new admin-only GET endpoint served by existing Express app.
- **Admin user flow:** `/ops` dashboard page shows content pipeline backlog; admins can monitor memorization/article processing failures.
- **API contract:** `contracts/api.md` now matches deployed backend routes.
- **Background jobs:** No worker changes in this commit.

## Impact And Risks

- **Impact:** Admin observability improved; no learner-facing change; no DB schema change in this commit.
- **Risk:** Low — new read-only admin endpoint; no state mutation.
- **Mitigation:** 3 new integration tests cover auth guard, shape, and edge cases.

## Untracked Files

None. All modified tracked files staged.

## Sensitive/Risk Review

- **Secrets:** None in diff.
- **Schema changes:** None in this commit (drill_feedback_jobs schema/migration is in Phase 3).
- **Auth/security changes:** None — existing admin middleware used unchanged.
- **Large files:** `.wolf/token-ledger.json` is large (~16K insertions) but is a runtime ledger file, not a secret. Included as-is per convention.

## Verification

```bash
cd backend && npm test
```
Result: 295 pass / 0 fail / 2 skipped (Postgres integration skipped, expected)

```bash
cd expat8-dashboard && npm run lint && npm run build
```
Result: lint clean; Next.js 15.5.18 build passed

```bash
cd mobile && flutter test test/speaking_test.dart
```
Result: 19 pass / 0 fail

## Commit Comments

Phase 2 content pipeline health: adds GET /v1/admin/content-pipeline/health returning per-surface
pending/failed counts for articles, memorization passages, and shadowing videos. Admin ops
dashboard shows Content Pipeline card. Contracts updated with previously undocumented endpoints.
Mobile speaking test DB isolation fixed. Roadmap Phase 2 marked complete; Phase 3 proposal entry added.

Backend: 295 pass. Dashboard: lint + build clean. Mobile speaking tests: 19/19 pass.

## Notes

- Phase 3 OpenSpec change (phase-3-lightweight-ai-feedback) is in openspec/ which is gitignored — not included in this commit.
- 4 manual E2E gates for Phase 2 remain (Docker worker E2E, Android shadowing, memorization E2E, exam integration) — blocked on physical device/env.
