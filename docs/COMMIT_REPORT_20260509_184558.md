# Commit Report

## Summary

- Project: `/home/beou/IdeaProjects/expat8`
- Branch: `x51-commit/20260509-184558-expat8-dashboard-archive`
- Mode: single project
- Timestamp: `20260509-184558`
- Branch decision: new branch, because the worktree already contained many unrelated changes plus untracked generated/spec artifacts.
- Commit title: `chore: archive expat8 dashboard spec and sync deployment docs`
- Push target: `origin HEAD`

## Uncommitted Audit

- Dashboard rename and deployment work was already implemented locally and archived through OpenSpec.
- Main spec tree now includes `expat8-dashboard` and an updated `backend-container-deployment` spec.
- Root docs and compose wiring mention `expat8-dashboard`.
- `.wolf/*` files are noisy local metadata changes unrelated to the feature work.

## Files Planned For Commit

- `README.md`: mention the dashboard app in workspace structure and local deployment notes.
- `docker-compose.yml`: add the `expat8-dashboard` service beside backend and PostgreSQL.
- `expat8-dashboard/**`: standalone dashboard app source and build/deploy files.
- `openspec/specs/backend-container-deployment/spec.md`: normalize and sync Compose deployment spec.
- `openspec/specs/expat8-dashboard/spec.md`: add the main dashboard capability spec.
- `openspec/changes/archive/2026-05-09-admin-web-dashboard/**`: archived prior dashboard change.
- `openspec/changes/archive/2026-05-09-expat8-dashboard/**`: archived current change.
- `openspec/changes/content-ingestion-v2-foundation/design.md`: dashboard exploration note.
- `.wolf/*`: existing local metadata churn present in the worktree.

## Detailed Changes

- `expat8-dashboard/**`: renamed and packaged the admin dashboard as a standalone Next.js service with Docker and runtime config.
- `docker-compose.yml`: starts backend, PostgreSQL, and dashboard together with env-driven ports and credentials.
- `README.md`: documents the dashboard app and updated Compose usage.
- `openspec/specs/backend-container-deployment/spec.md`: now uses valid main-spec structure and includes dashboard deployment requirements.
- `openspec/specs/expat8-dashboard/spec.md`: new main spec for dashboard deployment and moderation workflow.
- `openspec/changes/archive/**`: archives both dashboard-related OpenSpec changes.

## Change Content Summary

- Behavior/config/docs/tests: dashboard deployment naming, Compose orchestration, runtime config, and OpenSpec spec synchronization.

## Affected Flows

- Runtime flow: dashboard boot, backend auth headers, DB hydration, Compose startup.
- User/API flow: article creation, article review, vocabulary review, publish actions.
- Background/deployment/dev workflow: Docker/Compose deployment and OpenSpec archive flow.

## Impact And Risks

- Impact: changes dashboard packaging and deployment docs, plus OpenSpec main specs.
- Risk: `.wolf/*` metadata files are noisy and may not be desirable in long-lived history.
- Mitigation/notes: all known worktree changes are staged together per request; no unrelated files were dropped.

## Untracked Files

- `expat8-dashboard/**`: staged as the dashboard app and build artifacts.
- `openspec/changes/archive/2026-05-09-admin-web-dashboard/**`: staged as archived OpenSpec history.
- `openspec/changes/archive/2026-05-09-expat8-dashboard/**`: staged as archived OpenSpec history.
- `openspec/specs/admin-web-dashboard/spec.md`: staged as the legacy dashboard spec.
- `openspec/specs/expat8-dashboard/spec.md`: staged as the new dashboard main spec.

## Sensitive/Risk Review

- Secrets checked: no `.env` or credential files were staged from this audit.
- Large/generated files: `expat8-dashboard/package-lock.json` and `.wolf/*` are generated/noisy and will be committed because they are already present in the worktree.
- Deleted files: none.

## Verification

```bash
git status --short
git diff --stat
git diff --name-only
git ls-files --others --exclude-standard
```

Result:

```text
Worktree contains dashboard implementation, OpenSpec archive/spec files, docs/compose updates, and unrelated .wolf metadata churn.
```

## Commit Comments

Archive and sync the dashboard deployment work into the main OpenSpec/spec tree, while preserving the local dashboard app, Compose wiring, and existing workspace metadata.

## Notes

- Remaining risk: `.wolf/*` files are included because they were already modified in the worktree and the commit workflow requires staging all untracked/modified files.
