# Commit Report

## Summary

- Project: `/home/beou/IdeaProjects/expat8`
- Branch: `x51-commit/20260509-155944-expat8-swipe-archive`
- Mode: single project
- Timestamp: 2026-05-09 15:59:44
- Branch decision: new branch, because the worktree contains a large mixed change set with code, specs, archive moves, and generated `.wolf` metadata.
- Commit title: `fix(mobile): align swipe right-to-left with new-word intent`
- Push target: `origin HEAD`

## Uncommitted Audit

- Mobile runtime behavior: right-to-left swipe now uses `newFirst` instead of `mixed`.
- Mobile tests: added regression coverage for right-to-left swipe behavior and fallback.
- OpenSpec spec: updated mobile learning-session behavior to reflect gesture single-flight, study-event sync, and swipe semantics.
- Archive moves: archived completed OpenSpec changes for swipe fixes.
- Docs: added swipe invariant investigation note.
- `.wolf/` metadata: local assistant bookkeeping changed during the session.

## Files Planned For Commit

- `mobile/lib/src/session/learning_session_controller.dart`: gesture mode fix.
- `mobile/test/learning_session_controller_test.dart`: regression tests.
- `openspec/specs/mobile-learning-session/spec.md`: spec alignment.
- `docs/20260509-swipe-right-to-left-new-word-invariant.md`: investigation note.
- `openspec/changes/archive/2026-05-09-fix-swipe-right-to-left-new-word/*`: archive completed change.
- `openspec/changes/archive/2026-05-09-harden-mobile-swipe-activity/*`: archive completed change.
- `.wolf/anatomy.md`, `.wolf/buglog.json`, `.wolf/cron-state.json`, `.wolf/hooks/_session.json`, `.wolf/memory.md`, `.wolf/token-ledger.json`: assistant bookkeeping updates present in the worktree.

## Detailed Changes

- `mobile/lib/src/session/learning_session_controller.dart`: changed `onSwipeRightToLeft()` to request `newFirst`, so the gesture consistently prefers new words.
- `mobile/test/learning_session_controller_test.dart`: added coverage that right-to-left swipes keep yielding words beyond the old 3-card window and fall back to non-mastered cards when no new words remain.
- `openspec/specs/mobile-learning-session/spec.md`: updated the learning-session spec to describe study-event persistence, gesture single-flight, and the intended swipe semantics.
- `docs/20260509-swipe-right-to-left-new-word-invariant.md`: recorded the investigation and root cause.
- `openspec/changes/archive/...`: moved completed OpenSpec changes into archive history.
- `.wolf/*`: runtime assistant state and token ledgers were updated during exploration and testing.

## Change Content Summary

- Behavior/config/docs/tests: one gesture routing fix, one regression test addition, spec updates, archival cleanup, and session notes.

## Affected Flows

- Runtime flow: right-to-left swipe selection in the mobile learning session.
- User/API flow: card selection behavior on the learning screen.
- Background/deployment/dev workflow: OpenSpec archive structure and local assistant bookkeeping.

## Impact And Risks

- Impact: users can keep swiping right-to-left for new words without the old 3-card cap.
- Risk: `.wolf/*` files are local automation metadata and may churn independently of product changes.
- Mitigation/notes: the functional code change is narrow; regression tests cover the intended swipe behavior and fallback path.

## Untracked Files

- `docs/20260509-swipe-right-to-left-new-word-invariant.md`: staged for commit.
- `openspec/changes/archive/2026-05-09-fix-swipe-right-to-left-new-word/*`: staged for commit.
- `openspec/changes/archive/2026-05-09-harden-mobile-swipe-activity/*`: staged for commit.

## Sensitive/Risk Review

- Secrets checked: no secret-looking files were added in this commit set.
- Large/generated files: `.wolf/token-ledger.json` is large and machine-generated; included because it is already in the worktree.
- Deleted files: archive source directory under `openspec/changes/harden-mobile-swipe-activity/` was removed as part of the archive move.

## Verification

```bash
adb install -r "mobile/build/app/outputs/flutter-apk/app-release.apk"
adb shell monkey -p com.example.expat8_language_app -c android.intent.category.LAUNCHER 1
adb logcat -c && adb shell input swipe 260 320 60 320 300
```

Result:

```text
APK installed successfully; app launched on emulator-5554; no fatal startup errors observed; swipe gesture executed; UI remained healthy.
```

## Commit Comments

Align swipe semantics so right-to-left always requests a new word, and back it with regression coverage plus spec updates. Archive the completed OpenSpec work and preserve the investigation note for traceability.

## Notes

- Remaining risk: `.wolf/*` files may continue to change if the assistant runs more tooling before the commit completes.
