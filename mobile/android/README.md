# Android Platform Scaffold

Flutter SDK was unavailable in this environment, so generated Android platform files are not checked in yet.

Run this from `mobile/` after installing Flutter:

```bash
flutter create --platforms=android .
```

Keep the existing `lib/`, `test/`, `pubspec.yaml`, and `analysis_options.yaml` files.
